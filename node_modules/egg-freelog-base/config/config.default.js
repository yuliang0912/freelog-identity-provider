'use strict';

module.exports = appInfo => {
    const config = {};

    config.freelogBase = {
        retCodeEnum: {},
        errCodeEnum: {}
    }

    config.uploadConfig = {
        aliOss: {
            enable: false,
        },
        amzS3: {
            enable: false,
        }
    }

    return config;
};
